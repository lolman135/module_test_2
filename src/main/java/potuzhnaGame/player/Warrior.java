package potuzhnaGame.player;

import potuzhnaGame.Arena;

public class Warrior extends Character{

    public Warrior(String name, int maxHealth, int x, int y, int currentDamage, Arena arena) {
        super(name, maxHealth, x, y, currentDamage, arena);
    }
}
